---
description: Lisa GPT Live — client delegation, no video, browser mic
globs: web/src/lib/live/**,web/src/app/api/session/**,web/src/app/api/delegate/**,web/src/lib/enrichment/**
alwaysApply: false
---

# GPT Live (client delegation)

Session: `model: gpt-live-1`, `delegation: { type: "client" }`.
Cannot switch to Responses mid-session.

```ts
// session.input_transcript.delta     → keep in the app (delegation has no text)
// session.delegation.created {id}    → thinking.append immediately, POST /api/delegate
// wait until output-transcript idle  → commentary.append (verbatim). Else swallowed.
// hall packet (~17s) uses the same idle gate. Local Exa is the 1–3s fast lane.
```

The live agent is **Mac**. Process the room naturally; “Mac” is an optional address, not a mute gate.
Voice is `marin`. Personality is `LIVE_INSTRUCTIONS` (cheerleader / Gen Z), not a second voice.
Camera never reaches Live. Browser `getUserMedia({ audio: true })` is the default path.
Glasses ingest is additive (`noteGlassesSeen`); if silent, `health.input === "browser"`.

Exa write-back is **Jake** (`enrichment/**`, `memory/enrich.ts`). Do not `await` enrichment before Live speaks.
P0 remaining for Lisa: first+last name capture + tape. Docs: `docs/SHIP.md`.
Hermes `send` only if `HERMES_ENABLED=1` *and* hall is already up; never room `cos`.
