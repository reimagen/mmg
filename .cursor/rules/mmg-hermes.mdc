---
description: Hall / Hermes slow plane — fire-and-forget, never on Live path
globs: web/src/lib/hall/**,web/src/lib/enrichment/**,web/src/app/api/hall/**
alwaysApply: false
---

# Hermes + hall

Slow plane only. **P1** — local Exa is the P0 research path (`docs/SHIP.md`).
Do not stand up hall from scratch in the remaining hours.

- `HERMES_ENABLED=0` (default): local Exa/treg queue.
- `1` + `HALL_WS_URL`: `send("research", text)` fire-and-forget. Ack = row stored, not "research done."
- Hall down → same local queue. Conversation continues.
- On `packet`: paint `say` on the card; `commentary.append` only after output-transcript idle (LIVE-0002). UI card is verbatim. `consumed` after the card is on screen.
- Never send to room `cos`. Use `research`.
- Building block, not the submission. Plan: `docs/HERMES.md`. Loop: `docs/LIVE_LOOP.md`.
